apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: todo-app
spec:
  clusterIP: None  # Headless service
  selector:
    app: mysql
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
